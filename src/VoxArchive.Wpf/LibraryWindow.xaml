<Window x:Class="VoxArchive.Wpf.LibraryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ライブラリ"
        Width="1200"
        Height="760"
        MinWidth="1000"
        MinHeight="680"
        FontFamily="Yu Gothic UI"
        Background="#0A0D12">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBg" Color="#121821" />
        <SolidColorBrush x:Key="PanelBorder" Color="#283242" />
        <SolidColorBrush x:Key="ControlBg" Color="#1A2330" />
        <SolidColorBrush x:Key="ControlBorder" Color="#314055" />
        <SolidColorBrush x:Key="TextPrimary" Color="#F3F6FA" />
        <SolidColorBrush x:Key="TextMuted" Color="#99A7BD" />
        <SolidColorBrush x:Key="Accent" Color="#E94545" />
        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#2A5EBD" />
        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="White" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="Background" Value="{StaticResource ControlBg}" />
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Margin" Value="0,0,8,0" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="Background" Value="{StaticResource ControlBg}" />
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Height" Value="30" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource ControlBg}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="RowBackground" Value="#111722" />
            <Setter Property="AlternatingRowBackground" Value="#151D2B" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HeadersVisibility" Value="Column" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Foreground" Value="#111827" />
            <Setter Property="Background" Value="#E5E7EB" />
            <Setter Property="BorderBrush" Value="#CBD5E1" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </Window.Resources>

    <Grid Margin="14,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="*" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PanelBg}" BorderBrush="{StaticResource PanelBorder}" BorderThickness="1" CornerRadius="8" Padding="10">
            <DockPanel LastChildFill="False">
                <Button Content="更新" Command="{Binding RefreshCommand}" DockPanel.Dock="Left" />
                <Button Content="ファイル追加" Command="{Binding AddFileCommand}" DockPanel.Dock="Left" />
                <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource TextMuted}" />
            </DockPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.2*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource PanelBg}" BorderBrush="{StaticResource PanelBorder}" BorderThickness="1" CornerRadius="8" Padding="8">
                <DataGrid ItemsSource="{Binding Items}"
                          SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionUnit="FullRow">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="タイトル" Binding="{Binding Title}" Width="2*" />
                        <DataGridTextColumn Header="ファイル名" Binding="{Binding FileName}" Width="2*" />
                        <DataGridTextColumn Header="長さ" Binding="{Binding DurationText}" Width="80" />
                        <DataGridTextColumn Header="更新日時" Binding="{Binding UpdatedText}" Width="160" />
                        <DataGridTextColumn Header="サイズ" Binding="{Binding SizeText}" Width="90" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="2" Background="{StaticResource PanelBg}" BorderBrush="{StaticResource PanelBorder}" BorderThickness="1" CornerRadius="8" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="再生" FontWeight="Bold" FontSize="15" Margin="0,0,0,8" />

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="{Binding PlaybackButtonText}" Command="{Binding TogglePlaybackCommand}" Width="110" />
                        <Button Content="停止" Command="{Binding StopCommand}" Width="80" Margin="0,0,0,0" />
                    </StackPanel>

                    <TextBlock Grid.Row="2" Text="{Binding PositionText}" Foreground="{StaticResource TextMuted}" Margin="0,0,0,4" />
                    <Slider Grid.Row="3" Minimum="0" Maximum="{Binding DurationSeconds}" Value="{Binding SeekSeconds, Mode=TwoWay}" PreviewMouseLeftButtonDown="OnSeekDragStart" PreviewMouseLeftButtonUp="OnSeekDragEnd" Margin="0,0,0,10" />

                    <Grid Grid.Row="4" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="70" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Speaker Gain (dB)" Foreground="{StaticResource TextMuted}" />
                            <Slider Minimum="-60" Maximum="24" TickFrequency="3" IsSnapToTickEnabled="False" Value="{Binding SpeakerGainDb, Mode=TwoWay}" />
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="{Binding SpeakerGainDb, StringFormat={}{0:F1}}" HorizontalAlignment="Right" />
                    </Grid>

                    <Grid Grid.Row="5" Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="70" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Mic Gain (dB)" Foreground="{StaticResource TextMuted}" />
                            <Slider Minimum="-60" Maximum="24" TickFrequency="3" IsSnapToTickEnabled="False" Value="{Binding MicGainDb, Mode=TwoWay}" />
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="{Binding MicGainDb, StringFormat={}{0:F1}}" HorizontalAlignment="Right" />
                    </Grid>

                    <StackPanel Grid.Row="6">
                        <TextBlock Text="編集" FontWeight="Bold" FontSize="15" Margin="0,0,0,8" />
                        <TextBlock Text="タイトル" Foreground="{StaticResource TextMuted}" />
                        <TextBox Text="{Binding EditableTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,8" />
                        <Button Content="タイトル保存" Command="{Binding SaveTitleCommand}" Margin="0,0,0,8" />

                        <TextBlock Text="ファイル名" Foreground="{StaticResource TextMuted}" />
                        <TextBox Text="{Binding EditableFileName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,8" />
                        <Button Content="リネーム" Command="{Binding RenameCommand}" Margin="0,0,0,8" />

                        <StackPanel Orientation="Horizontal">
                            <Button Content="一覧から削除" Command="{Binding RemoveFromListCommand}" Margin="0,0,8,0" />
                            <Button Content="ファイル削除" Command="{Binding DeleteFileCommand}" Background="#5C232A" BorderBrush="#7A2D36" Margin="0" />
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="4" Text="一覧から削除はファイル実体を削除しません。" Foreground="{StaticResource TextMuted}" />
    </Grid>
</Window>
