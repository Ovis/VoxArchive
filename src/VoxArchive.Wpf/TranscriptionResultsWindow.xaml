<Window x:Class="VoxArchive.Wpf.TranscriptionResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="文字起こし結果"
        Width="920"
        Height="680"
        MinWidth="620"
        MinHeight="420"
        WindowStyle="None"
        ResizeMode="CanResize"
        FontFamily="Yu Gothic UI"
        Background="#0B1018">
    <WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="6"
                            CornerRadius="0"
                            UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="ControlBg" Color="#1A2636" />
        <SolidColorBrush x:Key="ControlBorder" Color="#35506E" />
        <SolidColorBrush x:Key="TextPrimary" Color="#EFF4FC" />
        <SolidColorBrush x:Key="TextMuted" Color="#95A7C0" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="TitleBarGlyphButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="20" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="#3A4A60" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#D9E2F0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Base"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Base" Property="Background" Value="#1D2A3A" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Base" Property="Background" Value="#16202D" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="28" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#10161F"
                MouseLeftButtonDown="OnTitleBarMouseLeftButtonDown">
            <Grid Margin="10,0,6,0">
                <TextBlock Text="文字起こし結果"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextMuted}" />
                <Button HorizontalAlignment="Right"
                        Style="{StaticResource TitleBarGlyphButtonStyle}"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="10"
                        Content="&#xE711;"
                        ToolTip="閉じる"
                        Click="OnCloseButtonClick" />
            </Grid>
        </Border>

        <Border Grid.Row="1"
                Margin="16"
                Background="#121B27"
                BorderBrush="#243244"
                BorderThickness="1"
                CornerRadius="12"
                Padding="16">
            <local:LibraryTranscriptionResultsPanel xmlns:local="clr-namespace:VoxArchive.Wpf"
                                                    x:Name="ResultsPanel"
                                                    Margin="0"
                                                    ShowHeader="False"
                                                    TranscriptHeight="Auto" />
        </Border>
    </Grid>
</Window>
