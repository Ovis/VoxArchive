<Window x:Class="VoxArchive.Wpf.SpeechRegionDetectorAdvancedSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VoxArchive.Wpf"
        Title="発話検出の詳細設定"
        Width="430"
        Height="360"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Silero VAD 詳細設定" FontSize="16" FontWeight="SemiBold" />
            <TextBlock Margin="0,4,0,12"
                       Foreground="{DynamicResource TextMuted}"
                       TextWrapping="Wrap"
                       Text="ここでの変更は、親の設定画面で「保存」を押すまで永続化されません。" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="150" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" VerticalAlignment="Center" Text="最小発話時間" />
                <local:NumericUpDownControl x:Name="MinimumSpeechNumericUpDown" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" />

                <TextBlock Grid.Row="1" VerticalAlignment="Center" Text="最小無音時間" />
                <local:NumericUpDownControl x:Name="MinimumSilenceNumericUpDown" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" />

                <TextBlock Grid.Row="2" VerticalAlignment="Center" Text="前方パディング" />
                <local:NumericUpDownControl x:Name="PrePaddingNumericUpDown" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" />

                <TextBlock Grid.Row="3" VerticalAlignment="Center" Text="後方パディング" />
                <local:NumericUpDownControl x:Name="PostPaddingNumericUpDown" Grid.Row="3" Grid.Column="1" />
            </Grid>

            <Button Margin="0,14,0,0"
                    HorizontalAlignment="Left"
                    Content="既定値に戻す"
                    Click="OnResetDefaultsClick" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Width="90" Margin="0,0,8,0" Content="OK" IsDefault="True" Click="OnOkClick" />
            <Button Width="90" Content="キャンセル" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
