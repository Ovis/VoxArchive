<Window x:Class="VoxArchive.Wpf.CloseToTrayNoticeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="500"
        MinWidth="460"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        ShowInTaskbar="False">
    <Border Margin="10" CornerRadius="12" Background="#121821" BorderBrush="#2B394D" BorderThickness="1" Padding="18,16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="ウィンドウを閉じてもアプリは終了しません"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="#F3F6FA"
                       Margin="0,0,0,10" />

            <TextBlock Grid.Row="1"
                       TextWrapping="Wrap"
                       Foreground="#D8E0ED"
                       FontSize="13"
                       LineHeight="20"
                       Margin="0,0,0,14"
                       Text="×ボタンを押すと、VoxArchive は終了せず通知領域（タスクトレイ）へ格納されます。終了する場合は通知領域のアイコンを右クリックし、[終了] を選択してください。" />

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <CheckBox x:Name="DontShowAgainCheckBox"
                          Style="{StaticResource CommonLabeledCheckBoxStyle}"
                          VerticalAlignment="Center"
                          Content="今後この説明を表示しない"
                          Foreground="#D8E0ED" />

                <Button Grid.Column="1"
                        MinWidth="96"
                        Padding="14,7"
                        Margin="12,0,0,0"
                        Background="#2A5EBD"
                        BorderBrush="#2A5EBD"
                        BorderThickness="1"
                        Foreground="White"
                        FontWeight="SemiBold"
                        IsDefault="True"
                        IsCancel="True"
                        Click="OnOkClick"
                        Content="OK" />
            </Grid>
        </Grid>
    </Border>
</Window>
