<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="CommonGridCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid Width="18" Height="18">
                        <Border x:Name="Box"
                                Background="#0F1A29"
                                BorderBrush="#35506E"
                                BorderThickness="1"
                                CornerRadius="4" />
                        <Path x:Name="Mark"
                              Data="M 4 9 L 7 12 L 13 6"
                              Stroke="White"
                              StrokeThickness="2"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"
                              Visibility="Collapsed" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="#2F6FD8" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="#4B86E6" />
                            <Setter TargetName="Mark" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="#4B86E6" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Box" Property="Background" Value="#162131" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="#25364C" />
                            <Setter TargetName="Box" Property="Opacity" Value="0.8" />
                            <Setter TargetName="Mark" Property="Opacity" Value="0.7" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CommonLabeledCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Foreground" Value="#EFF4FC" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="18" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border x:Name="Box"
                                Grid.Column="0"
                                Width="18"
                                Height="18"
                                CornerRadius="4"
                                Background="#0F1A29"
                                BorderBrush="#35506E"
                                BorderThickness="1"
                                VerticalAlignment="Center" />
                        <Path x:Name="Mark"
                              Grid.Column="0"
                              Data="M 4 9 L 7 12 L 13 6"
                              Stroke="White"
                              StrokeThickness="2"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"
                              Visibility="Collapsed"
                              VerticalAlignment="Center"
                              Margin="0,-2,0,0" />
                        <ContentPresenter Grid.Column="2"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="#2F6FD8" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="#4B86E6" />
                            <Setter TargetName="Mark" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="#4B86E6" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#7F8CA3" />
                            <Setter TargetName="Box" Property="Background" Value="#162131" />
                            <Setter TargetName="Box" Property="BorderBrush" Value="#25364C" />
                            <Setter TargetName="Box" Property="Opacity" Value="0.8" />
                            <Setter TargetName="Mark" Property="Opacity" Value="0.7" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
